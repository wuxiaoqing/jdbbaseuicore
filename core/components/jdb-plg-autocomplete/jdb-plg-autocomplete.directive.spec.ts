import { JdbPlgAutocompleteDirective } from './jdb-plg-autocomplete.directive';

describe('JdbPlgAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new JdbPlgAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
