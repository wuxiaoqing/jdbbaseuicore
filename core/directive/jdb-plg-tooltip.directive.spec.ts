import { JdbPlgTooltipDirective } from './jdb-plg-tooltip.directive';

describe('JdbPlgTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new JdbPlgTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
